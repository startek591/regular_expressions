const str = "Hello world. HiS This is Leela";

const regex = new RegExp("s", "gi");

console.log(regex.exec(str));
console.log(regex.exec(str));
console.log(regex.exec(str));
console.log(regex.exec(str));
